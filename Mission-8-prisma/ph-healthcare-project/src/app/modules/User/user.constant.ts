export const userSearchableFields: string[] = ["email"]; // only for searchTerm
export const userFilterableFields: string[] = [
  "email",
  "role",
  "status",
  "searchTerm",
]; // for all filtering

export const userOptionFields = ["limit", "page", "sortBy", "sortOrder"]; // options like pagination and sorting
